{
  "name": "wasm-playground",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "build": "gulp build",
    "build:dev": "gulp buildDev",
    "watch": "gulp buildDev && gulp watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "zoron (https://github.com/fralonra/)",
  "license": "ISC",
  "devDependencies": {
    "gulp": "^4.0.2",
    "gulp-autoprefixer": "^7.0.1",
    "gulp-cssmin": "^0.2.0",
    "gulp-less": "^4.0.1",
    "gulp-terser": "^1.2.0"
  }
}
